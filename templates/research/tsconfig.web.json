{
  // Typecheck-only config for the DOM sources — the web browser app + the
  // Electron renderer view (React). The bundlers (vite / electron-vite) BUILD
  // them via esbuild (no typecheck); run `tsc -p tsconfig.web.json` to typecheck
  // them in your editor / CI. The Node `tsconfig.json` compiles the cli boot +
  // web serve host; tsconfig.electron.json covers the Electron main + preload.
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    // `node` too: the brief's vocabulary and the interface's fold name the app's `Config`, a type derived from the
    // table in `app.ts`, and following that type-only import lands tsc in the app's Node imports. Nothing Node reaches
    // the bundle — a type import is erased — and the browser bundle's own import check is step 4's.
    "types": ["vite/client", "node"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": [
    "src/brief/protocol.ts",
    "src/research/budgets.ts",
    "src/research/prompts.ts",
    "src/ui/**/*",
    "types/**/*",
    "targets/desktop/view.tsx",
    "targets/web/main.tsx",
    "targets/web/boot.ts",
    "targets/web/web-bridge.ts"
  ],
  // `src/ui/**/*` sweeps the whole view directory, and the terminal view lives
  // there too — it is Ink, not DOM, and the Node config above owns it.
  "exclude": ["src/ui/cli.tsx"]
}
