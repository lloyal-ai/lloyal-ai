/** The content plane's client half is media's: every door derived from the one
 *  origin the bridge reports — which a view reads with `useContentOrigin()`
 *  from `@lloyal-labs/ui`, `null` on a bridge with no plane (cli): the view then
 *  offers no attach control and names an attachment instead of showing it. */
export {
  configUrl, ingestMedia, ingressUrl, manifestUrl, parseAttachmentHref, representationUrl, resolvePrefix, sourceUrl,
} from "@lloyal-labs/media";

